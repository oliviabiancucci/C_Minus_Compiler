/* 
  A program with no syntatic or semantic errors.
  Meant to test simple arithmetic, looping, and input/output functions.
*/
void main (void) 
{
  int x; 
  int y;

  x = input();
  y = 0;
  
  while (y < x) 
  {
    y = y + 3;
  } 
  
  output(y);
}
