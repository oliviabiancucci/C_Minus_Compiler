/* 
    A program with no syntatic or semantic errors.
    Meant to test simple arithmetic, negative numbers, function calls, and nested if statments.
*/
int add(int x, int y)
{
  return x + y;
}

int main (void) {
  int x; 
  int y;
  int fac;
  bool z;

  fac = -2;
  y = 7;
  x = add(fac, y);
  z = true;

  if(fac < 0)
  {
    if(y > 0)
    {
      fac = 420;
    }
  }
  else{
    fac = 69;
  }

  output(fac);
}
