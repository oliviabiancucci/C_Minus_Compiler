/* Syntactic Errors:
    1. main function has no type
    2. while loop has no left parenthesis
*/

main (void) {
  int x; 
  int fac;

  x = input();
  fac = -1;
  while x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}
